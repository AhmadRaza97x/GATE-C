#include<stdio.h>

    // BASIC ARRAY
    // * ADDRESS KI VALUE
    // & ADDRESS

int main()
{
    int a=10;
    printf("%d",*&*&*&*&a);     // * AND & DONO DUSHMAN H YE EK DUSRE KO CANCEL KR DETE H

    return 0;
}
